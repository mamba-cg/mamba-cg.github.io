{
  "name": "mamba-cg-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static website builder for Mamba CG.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && python3 -m http.server 8000"
  }
}
